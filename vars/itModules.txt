the list of Maven module paths having integration tests, found in the current directory and sub directories.
We will return all modules ending in {@code -test-tests} and {@code -test-docker}
(e.g. {@code xwiki-platform-administration-test-tests} and {@code xwiki-platform-administration-test-docker}).